/// <summary>
/// The three root archetypes that all job classes derive from.
/// Hybrid jobs belong to multiple archetypes (e.g., Paladin = Warrior + Mage).
/// </summary>
public enum BaseArchetype
{
    Warrior = 0,
    Mage = 1,
    Rogue = 2
}
