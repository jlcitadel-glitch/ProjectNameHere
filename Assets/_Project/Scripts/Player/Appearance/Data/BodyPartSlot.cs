/// <summary>
/// Defines the layer slots for a modular LPC character.
/// Sort order determines default rendering order (higher = in front).
/// </summary>
public enum BodyPartSlot
{
    WeaponBehind = 0,
    Body = 1,
    Head = 2,
    Torso = 3,
    Legs = 4,
    Hair = 5,
    WeaponFront = 6
}
